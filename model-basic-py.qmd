# model-basic-py.qmd {#sec-model-basic-py}

The `model-basic-py.qmd` file creates a model using Python's `palmerpenguins`, `numpy`, `pandas`, `sklearn.linear_model` and `sklearn` libraries. 

<details>
  <summary>Expand to view model-basic-py.qmd</summary>
  
```{r}
#| label: print-eda-basic-qmd
#| echo: false
library(readr)
readr::read_lines("_labs/lab1/model-basic-py.qmd") |> 
  base::noquote() |> 
  writeLines()
```

</details>

```{r}
library(reticulate)
```


```{python}
#| eval: false
from palmerpenguins import penguins
from pandas import get_dummies
import numpy as np
from sklearn.linear_model import LinearRegression
from sklearn import preprocessing
```

```{python}
#| eval: false
df = penguins.load_penguins().dropna()

df.head(3)
```

```{python}
#| eval: false
X = get_dummies(df[['bill_length_mm', 'species', 'sex']], drop_first = True)
y = df['body_mass_g']

model = LinearRegression().fit(X, y)
```

```{python}
#| eval: false
print(f"R^2 {model.score(X,y)}")
print(f"Intercept {model.intercept_}")
print(f"Columns {X.columns}")
print(f"Coefficients {model.coef_}")
```

