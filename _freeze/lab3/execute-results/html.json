{
  "hash": "101642aedb797a3dd6f7b05ab62a3285",
  "result": {
    "engine": "knitr",
    "markdown": "# Lab 3 {.unnumbered}\n\n\n\n\n\n\n\n\n:::: {.callout-warning collapse='false' appearance='default' icon='false'}\n\n## [Caution]{style='font-weight: bold; font-size: 1.25em;'}\n\n::: {style='font-size: 1.00em; color: #282b2d;'}\n\n\nThis section is being revised. Thank you for your patience.\n\n::: \n\n::::\n\n\n\n\n\nThe original files in `_labs/lab3` are below: \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfs::dir_tree(path = \"_labs/lab3/\")\n## _labs/lab3/\n## ├── app-api.R\n## ├── app-api.py\n## ├── eda-db.qmd\n## ├── model-db.qmd\n## ├── py-data-load.py\n## └── r-data-load.R\n```\n:::\n\n\n\n\n\nIn the previous labs, we build a Python model API with [`vetiver`](https://rstudio.github.io/vetiver-python/stable/) (also [available in R](https://rstudio.github.io/vetiver-r/)). This lab continues with accessing databases and using APIs. \n\n## APIs \n\nLet's back up a bit and get an understanding of what an API does. In general, APIs are the digital plumbing that makes modern software ecosystems possible. \n\n\n\n\n\n```{mermaid}\n%%| fig-cap: 'APIs'\n%%| fig-align: center\n%%{init: {'theme': 'neutral', 'look': 'handDrawn', 'themeVariables': { 'fontFamily': 'monospace', \"fontSize\":\"16px\"}}}%%\ngraph LR\n    DS[\"Data Scientist\"] --> Req[\"API Request\"]\n    Req --> ApiServ[\"API Server\"]\n    ApiServ --> DataProcMod[\"Data Processing/Model\"]\n    DataProcMod --> Res[\"Results\"]\n    Res --> ApiServ\n    ApiServ --> APIResp[\"API Response\"]\n    APIResp --> DS\n    \n    style ApiServ fill:#e1f5fe\n    style DataProcMod fill:#f3e5f5\n```\n\n\n\n\n\nAPIs have standardized language that allows different software systems to communicate and share functionality (without needing to understand how the internal implementation works).\n\n**What do they communicate?**\n\n1. What you can request (endpoints)   \n2. How to make the request (HTTP methods, parameters)   \n3. What you'll get back (response format)   \n4. When things go wrong (error codes)  \n\nWe previously turned our Python model into a `VetiverModel()`, saved it to a `pins` board, and then created a `VetiverAPI()`. I've also completed these steps in R, which can be found in [`_labs/lab2/R/model-vetiver-r.qmd`](https://github.com/mjfrigaard/do4ds-labs/tree/main/_labs/lab2/R/model-vetiver-r.qmd). \n\n\nThe sections below cover how to create and communicate with APIs.  I've split each lab into separate `R/` and `Python/` folders and included RStudio project files. \n\n## R App API\n\nThe R App API files can be found in the [`_labs/lab3/` folder](https://github.com/mjfrigaard/do4ds-labs/tree/main/_labs/lab3/). \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfs::dir_tree(path = \"_labs/lab3/\")\n## _labs/lab3/\n## ├── app-api.R\n## ├── eda-db.qmd\n## └── r-data-load.R\n```\n:::\n\n\n\n\n\n### eda-db.qmd {#sec-eda-db.qmd}\n\nThe [`eda-db.qmd`](_labs/lab3/eda-db.qmd) file has the EDA R code from lab 1. \n\nIt begins by loading the libraries: \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(DBI) # add this\nlibrary(duckdb) # add this\nlibrary(palmerpenguins) # add this\nlibrary(dplyr)\nlibrary(ggplot2)\nlibrary(dbplyr)\n```\n:::\n\n\n\n\n\nThe code below creates a connection to the database, but needs to be changed slightly so the `penguins` data is registered with `duckdb` (before the queries can be run).\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# load penguins data first\npenguins_data <- palmerpenguins::penguins\n# then connect\ncon <- DBI::dbConnect(\n  duckdb::duckdb(), \n  dbdir = \"my-db.duckdb\"\n)\n# register table\nduckdb_register(con, \"penguins_data\", penguins_data)\n# create table\ndbExecute(con, \"CREATE OR REPLACE TABLE penguins AS SELECT * FROM penguins_data\")\n# create df \ndf <- dbGetQuery(con, \"SELECT * FROM penguins\") |> na.omit()\n```\n:::\n\n\n\n\n\nThe table below displays the means of the measurements (body mass, flipper length, and bill length/depth) by species and sex:\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf %>%\n  group_by(species, sex) %>%\n  summarise(\n    across(\n        ends_with(\"mm\") | ends_with(\"g\"),\n      \\(x) mean(x, na.rm = TRUE)\n      )\n    ) %>%\n  dplyr::collect() %>%\n  knitr::kable()\n```\n:::\n\n\n\n\n\nThe graph displays the best-fit linear relationship between `bill_length_mm` and `body_mass_g` by `species`:\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf %>%\n  ggplot(\n    aes(\n    x = bill_length_mm, \n    y = body_mass_g, \n    color = species)\n    ) +\n  geom_point() + \n  geom_smooth(method = \"lm\")\n```\n:::\n\n\n\n\n\nAnd we finish by closing the connection to the database.\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nDBI::dbDisconnect(con)\n```\n:::\n\n\n\n\n\nTo view the original code in `eda-db.qmd`, expand the section below:\n\n<details>\n  <summary>Expand to view eda-db.qmd</summary>\n  \n\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n````\n---\ntitle: \"Penguins EDA\"\nformat:\n  html:\n    code-fold: true\n---\n\n## *Penguin Size and Mass by Sex and Species*\n\n```{r}\nlibrary(dplyr)\nlibrary(ggplot2)\nlibrary(dbplyr)\n\ncon <- DBI::dbConnect(\n  duckdb::duckdb(), \n  dbdir = \"my-db.duckdb\"\n  )\ndf <- dplyr::tbl(con, \"penguins\")\n```\n\n```{r}\ndf %>%\n  group_by(species, sex) %>%\n  summarise(\n    across(\n        ends_with(\"mm\") | ends_with(\"g\"),\n      \\(x) mean(x, na.rm = TRUE)\n      )\n    ) %>%\n  dplyr::collect() %>%\n  knitr::kable()\n```\n\n## *Penguin Size vs Mass by Species*\n\n```{r}\ndf %>%\n  ggplot(aes(x = bill_length_mm, y = body_mass_g, color = species)) +\n  geom_point() + \n  geom_smooth(method = \"lm\")\n```\n\n```{r}\nDBI::dbDisconnect(con)\n```\n````\n\n\n:::\n:::\n\n\n\n\n\n</details>\n\n### app-api.R \n\n`app-api.R` creates an R Shiny application that calls the `vetiver` API. The top of the file creates an `api_url`:\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\napi_url <- \"http://127.0.0.1:8080/predict\"\n```\n:::\n\n\n\n\n\n#### UI\n\nThe UI definition is below: \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nui <- fluidPage(\n  titlePanel(\"Penguin Mass Predictor\"),\n\n  # Model input values\n  sidebarLayout(\n    sidebarPanel(\n      sliderInput(\n        \"bill_length\",\n        \"Bill Length (mm)\",\n        min = 30,\n        max = 60,\n        value = 45,\n        step = 0.1\n      ),\n      selectInput(\n        \"sex\",\n        \"Sex\",\n        c(\"Male\", \"Female\")\n      ),\n      selectInput(\n        \"species\",\n        \"Species\",\n        c(\"Adelie\", \"Chinstrap\", \"Gentoo\")\n      ),\n      # Get model predictions\n      actionButton(\n        \"predict\",\n        \"Predict\"\n      )\n    ),\n\n    mainPanel(\n      h2(\"Penguin Parameters\"),\n      verbatimTextOutput(\"vals\"),\n      h2(\"Predicted Penguin Mass (g)\"),\n      textOutput(\"pred\")\n    )\n  )\n)\n```\n:::\n\n\n\n\n\n#### Server\n\nThe server definition is below: \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nserver <- function(input, output) {\n  # Input params\n  vals <- reactive(\n    list(\n      bill_length_mm = input$bill_length,\n      species_Chinstrap = input$species == \"Chinstrap\",\n      species_Gentoo = input$species == \"Gentoo\",\n      sex_male = input$sex == \"Male\"\n    )\n  )\n\n  # Fetch prediction from API\n  pred <- eventReactive(\n    input$predict,\n    httr2::request(api_url) |>\n      httr2::req_body_json(vals()) |>\n      httr2::req_perform() |>\n      httr2::resp_body_json(),\n    ignoreInit = TRUE\n  )\n\n  # Render to UI\n  output$pred <- renderText(pred()$predict[[1]])\n  output$vals <- renderPrint(vals())\n}\n```\n:::\n\n::: {.cell}\n\n```{.r .cell-code}\nshinyApp(ui = ui, server = server)\n```\n:::\n\n\n\n\n\n### r-data-load.R {#sec-r-data-load-r}\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncon <- DBI::dbConnect(duckdb::duckdb(), dbdir = \"my-db.duckdb\")\n\nDBI::dbWriteTable(con, \"penguins\", palmerpenguins::penguins)\n\nDBI::dbDisconnect(con)\n```\n:::\n\n\n\n\n\n\nTo view the original code in `r-data-load.R`, expand the section below:\n\n<details>\n  <summary>Expand to view r-data-load.R</summary>\n\n\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n```\ncon <- DBI::dbConnect(duckdb::duckdb(), dbdir = \"my-db.duckdb\")\nDBI::dbWriteTable(con, \"penguins\", palmerpenguins::penguins)\nDBI::dbDisconnect(con)\n```\n\n\n:::\n:::\n\n\n\n\n\n</details>\n\n## Python App API\n\nThe Python App API files can be found in the [`_labs/lab3/Python/` folder](https://github.com/mjfrigaard/do4ds-labs/tree/main/_labs/lab3/Python). \n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfs::dir_tree(path = \"_labs/lab3/Python/\")\n## _labs/lab3/Python/\n## ├── Python.Rproj\n## ├── app-api.py\n## ├── model-db.qmd\n## └── py-data-load.py\n```\n:::\n\n\n\n\n\n### model-db.qmd {#sec-model-db}\n\nThe [model-db.qmd](_labs/lab3/model-db.qmd) file contains the EDA from [lab 1](lab1.qmd).\n\n\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\nimport duckdb\nfrom pandas import get_dummies\nimport numpy as np\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn import preprocessing\n```\n:::\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\ncon = duckdb.connect('my-db.duckdb')\ndf = con.execute(\"SELECT * FROM penguins\").fetchdf().dropna()\ncon.close()\n\ndf.head(3)\n```\n:::\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\nX = get_dummies(df[['bill_length_mm', 'species', 'sex']], drop_first = True)\ny = df['body_mass_g']\n\nmodel = LinearRegression().fit(X, y)\n```\n:::\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\nprint(f\"R^2 {model.score(X,y)}\")\nprint(f\"Intercept {model.intercept_}\")\nprint(f\"Columns {X.columns}\")\nprint(f\"Coefficients {model.coef_}\")\n```\n:::\n\n\n\n\n\n<details>\n  <summary>Expand to view model-db.qmd</summary>\n  \n\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n````\n---\ntitle: \"Model\"\nformat:\n  html:\n    code-fold: true\n---\n\n```{python}\nimport duckdb\nfrom pandas import get_dummies\nimport numpy as np\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn import preprocessing\n```\n\n## Get Data\n```{python}\ncon = duckdb.connect('my-db.duckdb')\ndf = con.execute(\"SELECT * FROM penguins\").fetchdf().dropna()\ncon.close()\n\ndf.head(3)\n```\n\n## Define Model and Fit\n```{python}\nX = get_dummies(df[['bill_length_mm', 'species', 'sex']], drop_first = True)\ny = df['body_mass_g']\n\nmodel = LinearRegression().fit(X, y)\n```\n\n## Get some information\n```{python}\nprint(f\"R^2 {model.score(X,y)}\")\nprint(f\"Intercept {model.intercept_}\")\nprint(f\"Columns {X.columns}\")\nprint(f\"Coefficients {model.coef_}\")\n```\n````\n\n\n:::\n:::\n\n\n\n\n\n</details>\n\n### app-api.py {#sec-app-api-py}\n\n`app-api.py` creates an Python Shiny application that calls the `vetiver` API.\n\n#### UI \n\n\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\nfrom shiny import App, render, ui, reactive\nimport requests\n\napi_url = 'http://127.0.0.1:8080/predict'\n\napp_ui = ui.page_fluid(\n    ui.panel_title(\"Penguin Mass Predictor\"), \n    ui.layout_sidebar(\n        ui.panel_sidebar(\n            [ui.input_slider(\"bill_length\", \"Bill Length (mm)\", 30, 60, 45, step = 0.1),\n            ui.input_select(\"sex\", \"Sex\", [\"Male\", \"Female\"]),\n            ui.input_select(\"species\", \"Species\", [\"Adelie\", \"Chinstrap\", \"Gentoo\"]),\n            ui.input_action_button(\"predict\", \"Predict\")]\n        ),\n        ui.panel_main(\n            ui.h2(\"Penguin Parameters\"),\n            ui.output_text_verbatim(\"vals_out\"),\n            ui.h2(\"Predicted Penguin Mass (g)\"), \n            ui.output_text(\"pred_out\")\n        )\n    )   \n)\n```\n:::\n\n\n\n\n\n\n#### Server\n\n\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\ndef server(input, output, session):\n    @reactive.Calc\n    def vals():\n        d = {\n            \"bill_length_mm\" : input.bill_length(),\n            \"sex_Male\" : input.sex() == \"Male\",\n            \"species_Gentoo\" : input.species() == \"Gentoo\", \n            \"species_Chinstrap\" : input.species() == \"Chinstrap\"\n\n        }\n        return d\n    \n    @reactive.Calc\n    @reactive.event(input.predict)\n    def pred():\n        r = requests.post(api_url, json = vals())\n        return r.json().get('predict')[0]\n\n    @output\n    @render.text\n    def vals_out():\n        return f\"{vals()}\"\n\n    @output\n    @render.text\n    def pred_out():\n        return f\"{round(pred())}\"\n```\n:::\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\napp = App(app_ui, server)\n```\n:::\n\n\n\n\n\n<details>\n  <summary>Expand to view app-api.py</summary>\n\n\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n```\nfrom shiny import App, render, ui, reactive\nimport requests\n\napi_url = 'http://127.0.0.1:8080/predict'\n\napp_ui = ui.page_fluid(\n    ui.panel_title(\"Penguin Mass Predictor\"), \n    ui.layout_sidebar(\n        ui.panel_sidebar(\n            [ui.input_slider(\"bill_length\", \"Bill Length (mm)\", 30, 60, 45, step = 0.1),\n            ui.input_select(\"sex\", \"Sex\", [\"Male\", \"Female\"]),\n            ui.input_select(\"species\", \"Species\", [\"Adelie\", \"Chinstrap\", \"Gentoo\"]),\n            ui.input_action_button(\"predict\", \"Predict\")]\n        ),\n        ui.panel_main(\n            ui.h2(\"Penguin Parameters\"),\n            ui.output_text_verbatim(\"vals_out\"),\n            ui.h2(\"Predicted Penguin Mass (g)\"), \n            ui.output_text(\"pred_out\")\n        )\n    )   \n)\n\ndef server(input, output, session):\n    @reactive.Calc\n    def vals():\n        d = {\n            \"bill_length_mm\" : input.bill_length(),\n            \"sex_Male\" : input.sex() == \"Male\",\n            \"species_Gentoo\" : input.species() == \"Gentoo\", \n            \"species_Chinstrap\" : input.species() == \"Chinstrap\"\n\n        }\n        return d\n    \n    @reactive.Calc\n    @reactive.event(input.predict)\n    def pred():\n        r = requests.post(api_url, json = vals())\n        return r.json().get('predict')[0]\n\n    @output\n    @render.text\n    def vals_out():\n        return f\"{vals()}\"\n\n    @output\n    @render.text\n    def pred_out():\n        return f\"{round(pred())}\"\n\napp = App(app_ui, server)\n```\n\n\n:::\n:::\n\n\n\n\n\n</details>\n\n### py-data-load.py {#sec-py-data-load-py}\n\n\n\n\n\n::: {.cell}\n\n```{.python .cell-code  code-fold=\"false\"}\nimport duckdb\nfrom palmerpenguins import penguins\n\ncon = duckdb.connect('my-db.duckdb')\ndf = penguins.load_penguins()\ncon.execute('CREATE TABLE penguins AS SELECT * FROM df')\ncon.close()\n```\n:::\n\n\n\n\n\n<details>\n  <summary>Expand to view py-data-load.py</summary>\n\n\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n```\nimport duckdb\nfrom palmerpenguins import penguins\n\ncon = duckdb.connect('my-db.duckdb')\ndf = penguins.load_penguins()\ncon.execute('CREATE TABLE penguins AS SELECT * FROM df')\ncon.close()\n```\n\n\n:::\n:::\n\n\n\n\n\n</details>\n\n\n\n\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}